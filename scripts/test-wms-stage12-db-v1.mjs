import {readFileSync} from 'node:fs';import {createRequire} from 'node:module';const require=createRequire(import.meta.url);const {PGlite}=require(process.env.TEST_RUNTIME?`${process.env.TEST_RUNTIME}/node_modules/@electric-sql/pglite`:'@electric-sql/pglite');const db=new PGlite();const one=async q=>(await db.query(q)).rows?.[0]||null;try{await db.exec(`create role anon;create role authenticated;create role service_role bypassrls;create table public.products(id uuid primary key default gen_random_uuid(),sku text,name text,gtin text,gondola text,shelf text);create table public.orders(id uuid primary key default gen_random_uuid(),status text,total numeric default 0,external_status_updated_at timestamptz,updated_at timestamptz default now());create table public.order_items(id uuid primary key default gen_random_uuid(),order_id uuid references public.orders(id),product_id uuid references public.products(id),quantity numeric not null,sku_snapshot text,name_snapshot text);`);await db.exec(readFileSync('supabase/migrations/20260908130000_stage12_wms_foundation_v1.sql','utf8'));let r=await one(`select enabled,execution_mode,picking_enabled,checking_enabled,ready_release_enabled,fefo_enforced,require_independent_checker from public.fulfillment_runtime_config where id=1`);if(r.enabled!==false||r.execution_mode!=='off'||r.picking_enabled!==false||r.checking_enabled!==false||r.ready_release_enabled!==false||r.fefo_enforced!==false||r.require_independent_checker!==true)throw new Error('defaults_not_fail_closed');const p=(await one(`insert into public.products(sku,name,gtin,gondola,shelf) values('SKU1','Arroz','7891','G01','P01') returning id`)).id;const o=(await one(`insert into public.orders(status,total) values('confirmed',10) returning id`)).id;await db.exec(`insert into public.order_items(order_id,product_id,quantity,sku_snapshot,name_snapshot) values('${o}','${p}',2,'SKU1','Arroz')`);r=await one(`select public.create_fulfillment_from_order_v1('${o}') x`);if(r.x.error!=='fulfillment_runtime_disabled'||r.x.side_effect_performed!==false)throw new Error('disabled_runtime_wrote');if(Number((await one(`select count(*)::int n from public.fulfillment_orders`)).n)!==0)throw new Error('disabled_runtime_created_order');console.log('PASS: stage12 WMS migration applies and remains fail-closed.')}finally{await db.close()}